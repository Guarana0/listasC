#include <stdio.h>

int main(int argc, char** argv) {
	int num,div, cont = 0;
	
	while (num> 2) {
		cont++/
		printf("Digite um numero");
		scanf("%d", &num);
		div = (num /100) * 10;
	}
	printf("O resultado final e %d", &div);
	printf("Para esse resultado foi preciso %d", cont);
	
	return 0;
}
